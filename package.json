{
  "name": "kan-mcp",
  "version": "0.1.0",
  "main": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --compile --outfile kan-mcp",
    "test": "bun test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
